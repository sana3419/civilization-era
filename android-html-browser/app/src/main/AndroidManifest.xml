<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      存储方案：改用 SAF（Storage Access Framework）目录树。
      用户通过系统选择器挑一个文件夹，应用拿到可持久化的读权限，
      因此 **不需要任何存储权限**（无需 MANAGE_EXTERNAL_STORAGE / READ_*），更合规。

      故意不声明 INTERNET：从系统层面杜绝联网，只服务本地内容。
    -->

    <application
        android:name=".BrowserApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HtmlTabBrowser">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>
